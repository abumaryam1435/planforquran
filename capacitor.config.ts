import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quranmemorization.plan',
  appName: 'خطة حفظ القرآن',
  webDir: 'dist'
};

export default config;
